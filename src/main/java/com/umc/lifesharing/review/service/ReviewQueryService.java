package com.umc.lifesharing.review.service;

import com.umc.lifesharing.config.security.UserAdapter;

public interface ReviewQueryService {
    //boolean reviewDelete(Long reviewId, UserAdapter userAdapter);
}
